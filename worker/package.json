{
  "name": "pipeassessor-photo-worker",
  "private": true,
  "version": "1.0.0",
  "description": "Cloudflare Worker: authenticated upload/delete proxy for the finding-photos R2 bucket. Deployed separately from the main Vite app via Wrangler — not part of the app's build.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.0.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  }
}
